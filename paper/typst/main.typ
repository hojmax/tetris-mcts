#set document(title: "Bridge vs Runtime Draft")
#set page(width: 8.5in, height: 11in, margin: (x: 1in, y: 0.95in))
#set text(font: "Libertinus Serif", size: 11pt)
#set par(justify: true, leading: 0.65em)
#show figure.caption: set text(size: 10pt)

= Runtime and Bridge Tradeoff

This is a starter paper scaffold for visualizing the tradeoff between whole-game runtime and Bridge. The current figure uses fictitious data purely to lock in a layout and visual style before you replace it with real measurements.

#figure(
  image("../plots/bridge_vs_runtime_dummy.pdf", width: 100%),
  caption: [
    Dummy Pareto-style fronts over whole-game runtime and Bridge. Runtime is reversed so faster settings are further right. Marker shape encodes simulation budget.
  ],
) <fig:runtime-bridge>

Figure @fig:runtime-bridge is regenerated by `paper/build.sh`, which first rebuilds the PDF plot and then compiles this Typst document into `paper/build/paper.pdf`.

== Notes for Later

- Replace the placeholder front names with real experiment labels.
- Swap in real Bridge and runtime measurements.
- Keep the marker-by-simulation mapping if you want each point's search budget to stay readable without text labels on the plot itself.
