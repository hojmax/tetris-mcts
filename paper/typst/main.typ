#set document(title: "Average Attack vs Runtime")
#set page(width: 8.5in, height: 11in, margin: (x: 1in, y: 0.95in))
#set text(font: "Libertinus Serif", size: 11pt)
#set par(justify: true, leading: 0.65em)
#show figure.caption: set text(size: 10pt)

= Runtime and Average Attack

This figure compares average attack against whole-game runtime for several configurations.

#figure(
  image("../plots/avg_attack_vs_runtime.pdf", width: 100%),
  caption: [
    Average attack versus whole-game runtime. Lower runtime is better, so the x-axis is reversed. Marker shape encodes simulation budget.
  ],
) <fig:runtime-attack>

Figure @fig:runtime-attack is generated by `paper/build.sh`, which rebuilds the plot PDF and then compiles this Typst document into `paper/paper.pdf`.
