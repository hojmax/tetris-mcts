[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "tetris_core"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
features = ["extension-module"]
module-name = "tetris_core.tetris_core"
